const app = getApp();
let height = app.globalData.phoneheight
Page({

  /**
   * 页面的初始数据
   */
  data: {
    height:height
  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad: function (options) {
    
  },
})